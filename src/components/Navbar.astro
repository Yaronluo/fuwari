---
import { Icon } from "astro-icon/components";
import { navBarConfig, siteConfig, profileConfig } from "../config";
import { LinkPresets } from "../constants/link-presets";
import { LinkPreset, type NavBarLink } from "../types/config";
import { url } from "../utils/url-utils";
import LightDarkSwitch from "./LightDarkSwitch.svelte";
import Search from "./Search.svelte";
import DisplaySettings from "./widget/DisplaySettings.svelte";
import NavMenuPanel from "./widget/NavMenuPanel.astro";
import ImageWrapper from "./misc/ImageWrapper.astro";

const className = Astro.props.class;

let links: NavBarLink[] = navBarConfig.links.map(
	(item: NavBarLink | LinkPreset): NavBarLink => {
		if (typeof item === "number") {
			return LinkPresets[item];
		}
		return item;
	},
);
---
<div id="navbar" class="z-50 onload-animation">
    <div id="navbar-container" class:list={[
        className,
        "transition-all duration-500 w-full max-w-[var(--page-width)] h-14 mx-auto flex items-center justify-between pointer-events-none px-0"
        ]}>
        
        <!-- Left: Logo -->
        <div class="navbar-pill pointer-events-auto h-14 flex items-center relative shadow-md rounded-full px-4">
            <div class="nav-bg-blur absolute inset-0 rounded-full bg-white/40 dark:bg-black/30 backdrop-blur-xl z-[-1] transition-opacity duration-400"></div>
            <a href={url('/')} class="btn-plain scale-animation rounded-lg h-10 font-bold active:scale-95">
                <div class="flex flex-row text-[var(--primary)] items-center text-md">
                    <ImageWrapper src={profileConfig.avatar || ""} alt="Avatar" class="w-8 h-8 rounded-full mr-2" />
                    {siteConfig.title}
                </div>
            </a>
        </div>
        
        <!-- Middle: Links -->
        <div class="navbar-pill pointer-events-auto h-14 hidden md:flex relative items-center shadow-md rounded-full px-2" id="nav-menu-container">
            <div class="nav-bg-blur absolute inset-0 rounded-full bg-white/40 dark:bg-black/30 backdrop-blur-xl z-[-1] transition-opacity duration-400"></div>
            <div id="nav-indicator" class="absolute bottom-1 h-[3px] rounded-full bg-gradient-to-r from-transparent via-[var(--primary)] to-transparent transition-all duration-300 opacity-0 pointer-events-none"></div>
            {links.map((l) => {
                return <a aria-label={l.name} href={l.external ? l.url : url(l.url)} target={l.external ? "_blank" : null}
                          class="nav-link-item relative group h-10 font-bold px-4 flex items-center justify-center active:scale-95 transition-all"
                >
                    <div class="flex items-center text-black/75 dark:text-white/75 group-hover:text-[var(--primary)] transition-colors">
                        {l.icon && <Icon name={l.icon} class="text-[1.125rem] mr-1.5 transition-colors group-hover:text-[var(--primary)]" />}
                        {l.name}
                        {l.external && <Icon name="fa6-solid:arrow-up-right-from-square" class="text-[0.875rem] transition -translate-y-[1px] ml-1 text-black/[0.2] dark:text-white/[0.2] group-hover:text-[var(--primary)]"></Icon>}
                    </div>
                </a>;
            })}
        </div>
        
        <!-- Right: Actions -->
        <div class="navbar-pill pointer-events-auto h-14 flex items-center relative shadow-md rounded-full px-2">
            <div class="nav-bg-blur absolute inset-0 rounded-full bg-white/40 dark:bg-black/30 backdrop-blur-xl z-[-1] transition-opacity duration-400"></div>
            <!--<SearchPanel client:load>-->
            <Search client:only="svelte"></Search>
            {!siteConfig.themeColor.fixed && (
                    <button aria-label="Display Settings" class="btn-plain scale-animation rounded-lg h-10 w-10 active:scale-90" id="display-settings-switch">
                        <Icon name="material-symbols:palette-outline" class="text-[1.25rem]"></Icon>
                    </button>
            )}
            <button aria-label="Menu" name="Nav Menu" class="btn-plain scale-animation rounded-lg w-10 h-10 active:scale-90 md:!hidden" id="nav-menu-switch">
                <Icon name="material-symbols:menu-rounded" class="text-[1.25rem]"></Icon>
            </button>
        </div>
        <NavMenuPanel links={links}></NavMenuPanel>
        <DisplaySettings client:only="svelte"></DisplaySettings>
    </div>
</div>

<style is:global>
    #navbar-container.is-scrolled {
        background-color: rgba(255, 255, 255, 0.45) !important;
        backdrop-filter: blur(24px) !important;
        -webkit-backdrop-filter: blur(24px) !important;
        box-shadow: 0 4px 20px rgb(0 0 0 / 0.08) !important;
        padding-left: 1rem !important;
        padding-right: 1rem !important;
        pointer-events: auto !important;
        border-radius: 9999px !important;
    }
    :is(.dark) #navbar-container.is-scrolled {
        background-color: rgba(0, 0, 0, 0.35) !important;
    }
    #navbar-container.is-scrolled .navbar-pill {
        box-shadow: none !important;
    }
    #navbar-container.is-scrolled .nav-bg-blur {
        opacity: 0 !important;
    }
    .navbar-pill {
        transition: box-shadow 0.4s;
    }
</style>

<script>

function loadButtonScript() {
    let switchBtn = document.getElementById("scheme-switch");
    if (switchBtn) {
        switchBtn.onclick = function () {
            switchTheme()
        };
    }

    let settingBtn = document.getElementById("display-settings-switch");
    if (settingBtn) {
        settingBtn.onclick = function () {
            let settingPanel = document.getElementById("display-setting");
            if (settingPanel) {
                settingPanel.classList.toggle("float-panel-closed");
            }
        };
    }

    let menuBtn = document.getElementById("nav-menu-switch");
    if (menuBtn) {
        menuBtn.onclick = function () {
            let menuPanel = document.getElementById("nav-menu-panel");
            if (menuPanel) {
                menuPanel.classList.toggle("float-panel-closed");
            }
        };
    }

    const navContainer = document.getElementById('nav-menu-container');
    const indicator = document.getElementById('nav-indicator');
    const navLinks = document.querySelectorAll('.nav-link-item');

    if (navContainer && indicator && navLinks.length > 0) {
        navLinks.forEach(link => {
            link.addEventListener('mouseenter', () => {
                const rect = link.getBoundingClientRect();
                const containerRect = navContainer.getBoundingClientRect();
                const left = rect.left - containerRect.left;
                
                indicator.style.width = `${rect.width * 0.75}px`;
                indicator.style.transform = `translateX(${left + rect.width * 0.125}px)`;
                indicator.style.opacity = '1';
            });
        });

        navContainer.addEventListener('mouseleave', () => {
            indicator.style.opacity = '0';
        });
    }
}

loadButtonScript();
</script>

{import.meta.env.PROD && <script is:inline define:vars={{scriptUrl: url('/pagefind/pagefind.js')}}>
async function loadPagefind() {
    try {
        const response = await fetch(scriptUrl, { method: 'HEAD' });
        if (!response.ok) {
            throw new Error(`Pagefind script not found: ${response.status}`);
        }

        const pagefind = await import(scriptUrl);

        await pagefind.options({
            excerptLength: 20
        });

        window.pagefind = pagefind;

        document.dispatchEvent(new CustomEvent('pagefindready'));
        console.log('Pagefind loaded and initialized successfully, event dispatched.');
    } catch (error) {
        console.error('Failed to load Pagefind:', error);
        window.pagefind = {
            search: () => Promise.resolve({ results: [] }),
            options: () => Promise.resolve(),
        };
        document.dispatchEvent(new CustomEvent('pagefindloaderror'));
        console.log('Pagefind load error, event dispatched.');
    }
}

if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', loadPagefind);
} else {
    loadPagefind();
}
</script>}
